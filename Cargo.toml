[package]
name = "lane"
version = "0.0.0"
edition = "2021"
description = "Portable, offline-capable local agent-work orchestration — local lane claims, leases, target-overlap, and audit"
license = "MIT"
publish = false

[lib]
name = "lane"
path = "src/lib.rs"

[[bin]]
name = "lane"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = { version = "0.4", features = ["serde"] }
anyhow = "1"
# Slice 2: NFC normalization of target path segments for the overlap invariant.
# std has no Unicode NFC; this is the single new dependency (Commander-approved).
# Network-free, no async, no transitive HTTP/DB heft.
unicode-normalization = "0.1"
# Slice 4 (ZER-85): $LANE_ROOT/config.toml reader (secret role keys + [linear]
# settings) in src/config.rs. Parse-only, serde-native, network-free. The locking
# core never imports it (enforced by the source scan in tests/no_network_guard.rs).
toml = "0.8"
# Slice 4 (ZER-85): sync-only HTTP for the Linear GraphQL adapter (src/linear) —
# the crate's ONE network-capable dependency, allowlisted by name in
# tests/no_network_guard.rs (ADAPTER_ONLY). Blocking client with zero async
# runtime anywhere in its tree (tokio stays banned); rustls (no OpenSSL/system
# TLS linkage); json/gzip default features dropped (serde_json already in-tree;
# no Accept-Encoding wanted). The locking core never imports it (source scan).
ureq = { version = "3", default-features = false, features = ["rustls"] }

[dev-dependencies]
tempfile = "3"
assert_cmd = "2"
predicates = "3"
